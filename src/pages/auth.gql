query getClient($id:ID!){
    clients(query:{id:[$id]}){
        id
        url
    }
}

mutation createToken($client:ID!){
    createTokenGeneral(client:$client){
        id
        key
        createReq{
            token
            key
        }
    }
}