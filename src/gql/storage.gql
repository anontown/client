fragment stoage on Storage{
    key
    value
}

query findStorages($query:StorageQuery!){
    storages(query:$query){
        ...stoage
    }
}

mutation setStorage($key:String!,$value:String!){
    setStorage(key:$key,value:$value)
}