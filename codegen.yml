overwrite: true
schema: "generated/app.gql"
documents: "./src/**/*.gql"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript-common"
      - "typescript-client"
      - "typescript-react-apollo"
  src/generated/graphql.d.ts:
    plugins:
      - "typescript-graphql-files-modules"
config:
    scalars:
      DateTime: string
    withHooks: true
    noHOC: true
    noComponents: true